# `bin_to_ASCII`
비트씩 끊어서 들어오는 입력을 ASCII로 변환하여 출력하는 프로그램

- 입력: N개의 8비트 덩어리
- 출력: 이어진 ASCII문자

ASCII 테이블이 있어야 한다.
- `char` 타입의 배열이다.

1. 입력을 받아 몇 바이트가 들어왔는지 파악한다.
2. 메모리를 동적 할당하여 입력된 바이트들을 문자열 덩어리로써 저장한다.
3. 각 바이트에 대해 다음을 반복한다.
	1. 2진수 문자열을 정수로 변환한다.
	2. 변환된 정수를 ASCII 테이블의 인덱스로 사용해 문자를 출력한다.